diff --git a/src/ipm/termination.rs b/src/ipm/termination.rs
--- a/src/ipm/termination.rs
+++ b/src/ipm/termination.rs
@@ -304,9 +304,21 @@ fn dual_cone_ok(prob: &ProblemData, z: &[f64], tol: f64) -> bool {
             ConeSpec::NonNeg { dim } => {
                 if z[offset..offset + dim].iter().any(|&v| v < -tol) {
                     return false;
                 }
                 offset += dim;
             }
+            ConeSpec::Soc { dim } => {
+                let t = z[offset];
+                let mut x_norm2 = 0.0;
+                for xi in &z[offset + 1..offset + dim] {
+                    x_norm2 += xi * xi;
+                }
+                let x_norm = x_norm2.sqrt();
+                if t + tol < x_norm {
+                    return false;
+                }
+                offset += dim;
+            }
             _ => {
                 return false;
             }
